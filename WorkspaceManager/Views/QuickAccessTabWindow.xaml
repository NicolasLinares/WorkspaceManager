<UserControl x:Class="INVOXWorkspaceManager.Views.QuickAccessTabWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:INVOXWorkspaceManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600">

    <Control.Resources>
        <CollectionViewSource x:Key="items" Source="{Binding Items}">
        </CollectionViewSource>
        <CollectionViewSource x:Key="path" Source="{Binding Path}">
        </CollectionViewSource>
    </Control.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0" 
                 Name="PathListBox" 
                 VerticalAlignment="Center"
                 BorderBrush="Transparent"
                 ItemsSource="{Binding Source={StaticResource path}}"
                 SelectedValue="{Binding SelectedPathItem}"
                 SelectionChanged="ListBoxSelectionPathChanged"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 IsSynchronizedWithCurrentItem="True"
                 Focusable="False">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate >
                    <Grid Height="25" Background="LightBlue">
                        <TextBlock Margin="10px 0px"  Text="{Binding Name}" ToolTip="{Binding Description}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <ListBox Grid.Row="1" Name="FilesSourceListBox"
             ItemsSource="{Binding Source={StaticResource items}}"
             SelectedValue="{Binding SelectedItem}"
             SelectionChanged="ListBoxSelectionChanged"
             ScrollViewer.VerticalScrollBarVisibility="Disabled"
             IsSynchronizedWithCurrentItem="True"
             Focusable="False">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate >
                    <Grid  Width="120" Height="120" Background="LightBlue">
                        <TextBlock  Text="{Binding Name}" ToolTip="{Binding Description}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Height="40" Width="145" Click="NewQuickAccess_Click">
                <TextBlock>
                    New quick access
                </TextBlock>
            </Button>

            <Button Grid.Column="2" Height="40" Width="145" Click="NewFolder_Click">
                <TextBlock>
                    New folder
                </TextBlock>
            </Button>
        </Grid>
    </Grid>

</UserControl>
